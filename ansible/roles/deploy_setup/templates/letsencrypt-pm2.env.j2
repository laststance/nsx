# Written by Ansible. Sourced by certbot pre/post hooks so Snap's renew
# timer talks to the PM2 daemon that actually binds port 80.
HOME={{ certbot_pm2_home | dirname }}
PM2_HOME={{ certbot_pm2_home }}
